class Resursbank_CostList {
    static toggle(expanderEl) {
        console.log('I rule');
        // Get the content element (sitting next to expanderEl).
        expanderEl.parentElement.parentElement.classList.toggle('expanded');
    }
}
